# 🌍 МакедоТест — Progress Log

- Progress (2026-05-09 wave 9ck): editor canvas and paper shell reflow are refined in `src/main.jsx` to show more of the full test area at once; the main editor wrapper now uses tighter outer padding, the advanced-settings panel is more compact, and the `#test-paper` sheet is centered, slightly scaled down on desktop, and given a more premium sheet treatment with softer shadow, lighter borders, and tighter header/footer rhythm; no logic changes were introduced, and build PASS.
- Progress (2026-05-09 wave 9cj): grading threshold calculation is extracted from `src/main.jsx` into `src/features/editor/gradingScale.js` via `getGradingScaleThresholds(totalPoints)`; the `gradingScale` memo now uses the helper instead of inline `Math.ceil(totalPoints * ...)` object construction, with no runtime changes; `src/features/editor/gradingScale.test.js` is expanded to **5 tests** covering grade order, new array safety, and threshold rounding behavior; focused tests PASS (**1/1 file, 5/5 tests**), build PASS.
- Progress (2026-05-09 wave 9ci): school header value builder и parser logic се екстрахирани од `src/main.jsx` во нов helper модул `src/features/editor/headerInputs.js` преку `buildSchoolHeaderValue(testInfo)`, `parseSchoolHeaderValue(value)`, и `applySchoolHeaderInput(testInfo, value)`; schoolType+school input value, header preview text, и footer school label сега користат helper calls наместо inline string concatenation / split logic, без runtime промени; додаден `src/features/editor/headerInputs.test.js` со **4 тестови** (value building, empty-part handling, parse coverage, apply coverage); focused tests PASS (**1/1 file, 4/4 tests**), build PASS.
- Progress (2026-05-09 wave 9ch): toolbar import/export button class derivations се екстрахирани од `src/main.jsx` во нов helper модул `src/features/editor/toolbarButtons.js` преку `getJsonImportButtonClass()`, `getPasteButtonClass()`, `getQtiExportButtonClass()`, и `getQtiImportButtonClass()`; JSON import, paste AI, QTI export, и QTI import toolbar controls сега користат helper calls наместо inline class strings, без runtime промени; додаден `src/features/editor/toolbarButtons.test.js` со **4 тестови** (JSON/paste/QTI export/QTI import class coverage); focused tests PASS (**1/1 file, 4/4 tests**), build PASS.
- Progress (2026-05-09 wave 9cg): undo action button state-class derivation е екстрахиран од `src/main.jsx` во постоечки helper модул `src/features/editor/actionButtons.js` преку `getUndoButtonClass(canUndo)`; undo button class logic во editor toolbar сега користи helper call наместо inline ternary со долг class string, без runtime промени; `src/features/editor/actionButtons.test.js` е проширен на **9 тестови** со нов undo class coverage (enabled/disabled states); focused tests PASS (**1/1 file, 9/9 tests**), build PASS.
- Progress (2026-05-09 wave 9cf): navbar action-state derivations за Vision import и Save button се до-екстрахирани од `src/main.jsx` во постоечки helper модул `src/features/editor/actionButtons.js` преку `getVisionImportButtonClass(isVisionImporting)`, `getVisionImportButtonLabel(isVisionImporting)`, `getSaveButtonIconClass(isSaving)`, и `getSaveButtonLabel(isSaving, t)`; Vision label class/text и Save icon/text сега користат helper calls наместо inline ternary logic, без runtime промени; `src/features/editor/actionButtons.test.js` е проширен на **8 тестови** (save icon/label states + translator fallback, vision class/label states, постоечки language/save/collab coverage); focused tests PASS (**1/1 file, 8/8 tests**), build PASS.
- Progress (2026-05-09 wave 9ce): tutorial modal progress-dot class, next-step branching, и next-button label derivations се екстрахирани од `src/main.jsx` во нов helper модул `src/features/editor/tutorialUi.js` преку `getTutorialProgressDotClass(index, tutorialStep)`, `shouldAdvanceTutorialStep(tutorialStep, totalSteps)`, и `getTutorialNextButtonLabel(tutorialStep, totalSteps)`; tutorial render сега користи helper calls наместо inline ternary/conditional logic за dots и CTA label/flow, без runtime промени; додаден `src/features/editor/tutorialUi.test.js` со **4 тестови** (active/inactive dot class, step advancement detection, next/start label mapping); focused tests PASS (**1/1 file, 4/4 tests**), build PASS.
- Progress (2026-05-09 wave 9cd): toolbox item subject-indicator visibility и color-class derivation се екстрахирани од `src/main.jsx` во нов helper модул `src/features/editor/toolboxIndicators.js` преку `shouldShowSubjectIndicator(subjects)` и `getSubjectIndicatorClass(subjects)`; indicator dot render logic во toolbox cards сега користи helper calls наместо inline `subjects.includes(...)` checks и ternary class selection, без runtime промени; додаден `src/features/editor/toolboxIndicators.test.js` со **4 тестови** (visibility for stem/languages, hide behavior for non-target sets, color class derivation with stem precedence, fallback behavior); focused tests PASS (**1/1 file, 4/4 tests**), build PASS.
- Progress (2026-05-09 wave 9cc): category filter button class derivation и label extraction logic се екстрахирани од `src/main.jsx` во нов helper модул `src/features/editor/categoryButtons.js` преку `getCategoryButtonClass(isActive)` и `getCategoryButtonLabel(label)`; category filter button render блокот во sidebar сега користи helper calls наместо inline ternary class assembly и `label.split(' ')[0]` string operation, без runtime промени; додаден `src/features/editor/categoryButtons.test.js` со **5 тестови** (active/inactive class derivation, first-word label extraction, single-word labels, null/undefined safety); focused tests PASS (**1/1 file, 5/5 tests**), build PASS.
- Progress (2026-05-09 wave 9cb): paper content panel type determinants се екстрахирани од `src/main.jsx` во нов helper модул `src/features/editor/panelTypes.js` преку `getPaperPanelType(paperContentView)`, `isAnalyticsPanelType(panelType)`, `isVerifyPanelType(panelType)`, `isAnswerSheetPanelType(panelType)`, и `isQuestionsPanelType(panelType)`; вложена ternary логика за rendering различни content панели (analytics vs verify vs answerSheet vs questions) е рефакторирана во if-statement chain, без runtime промени; додаден `src/features/editor/panelTypes.test.js` со **5 тестови** (panel type identification, analytics panel detection, verify panel detection, answerSheet panel detection, questions panel detection); focused tests PASS (**1/1 file, 5/5 tests**), build PASS.
- Progress (2026-05-09 wave 9ca): paper display visibility и derivation helpers се екстрахирани од `src/main.jsx` во нов helper модул `src/features/editor/paperDisplay.js` преку `shouldShowGradingScale(testInfo)`, `shouldShowSmartBadge(subject)`, и `getSmartBadgeText(subject)`; grading scale paper section conditional (`testInfo.showScale &&`), smart badge visibility (`testInfo.subject.length > 5 &&`), и smart badge first-word extraction сега користат helper логика наместо inline conditionals и string operations, без runtime промени; додаден `src/features/editor/paperDisplay.test.js` со **3 тестови** (grading scale visibility for true/false/null states, smart badge visibility for subject length threshold, smart badge text extraction); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bz): view-based content determinants се екстрахирани од `src/main.jsx` во нов helper модул `src/features/editor/viewDeterminants.js` преку `isEditorView(view)`, `shouldShowTestFormFields(view)`, `shouldShowStudentLineFields(view)`, и `isAnalyticsView(paperContentView)`; header inputs (editor ternary), student line field conditional, и analytics panel detection сега користат helper логика наместо inline ternary и multi-part AND conditions, без runtime промени; додаден `src/features/editor/viewDeterminants.test.js` со **4 тестови** (editor view detection, test form field visibility, student line field visibility for multiple views, analytics view detection); focused tests PASS (**1/1 file, 4/4 tests**), build PASS.
- Progress (2026-05-08 wave 9by): modal visibility conditions се екстрахирани од `src/main.jsx` во нов helper модул `src/features/editor/modalVisibility.js` преку `shouldShowPasteModal(showPasteModal)` и `shouldShowTutorialModal(showTutorial)`; paste modal и tutorial modal render сега користат helper visibility проверки наместо inline truthiness checks, без runtime промени; додаден `src/features/editor/modalVisibility.test.js` со **4 тестови** (paste modal truthy/falsy, null/undefined handling, tutorial modal truthy/falsy, null/undefined handling); focused tests PASS (**1/1 file, 4/4 tests**), build PASS.
- Progress (2026-05-08 wave 9bx): alert and badge visibility conditions се екстрахирани од `src/main.jsx` во нов helper модул `src/features/editor/alertVisibility.js` преку `shouldShowDuplicateAlert(duplicateAlert)`, `shouldShowLastEditBadge(lastRemoteEdit)`, и `shouldShowConflictHint(conflictHint)`; navbar alerts (duplicate alert, last edit badge, conflict hint) сега користат helper visibility проверки наместо inline truthiness checks, без runtime промени; додаден `src/features/editor/alertVisibility.test.js` со **3 тестови** (duplicate alert truthy/falsy, last edit badge truthy/falsy, conflict hint truthy/falsy); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bw): floating action button (FAB) visibility condition е екстрахирана од `src/main.jsx` во нов helper модул `src/features/editor/fabState.js` преку `shouldShowAddQuestionFAB(sidebarOpen, view)`; FAB ќе се прикажува само кога sidebar е затворена (closed) и поглед е 'editor', наместо inline `!sidebarOpen && view === 'editor'`, без runtime промени; додаден `src/features/editor/fabState.test.js` со **2 тестови** (FAB visibility for all view/sidebar combinations, negative cases coverage); focused tests PASS (**1/1 file, 2/2 tests**), build PASS.
- Progress (2026-05-08 wave 9bv): editor/preview toggle button icon type и label derivations се екстрахирани од `src/main.jsx` во нов helper модул `src/features/editor/editorToggle.js` преку `getEditorToggleIconType(view)` и `getEditorToggleLabel(view, t)`; toggle button render сега користи helper резултати (`iconType === 'preview'` selector + label translation) наместо inline ternary за icon + label, без runtime промени; додаден `src/features/editor/editorToggle.test.js` со **3 тестови** (icon type selection, label translation, fallback translation); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bu): sidebar visibility/content derivation е екстрахирана од `src/main.jsx` во нов helper модул `src/features/editor/sidebarState.js` преку `getSidebarContainerClass(isOpen)` и `getSidebarContentClass(isOpen)`; sidebar aside container (`w-80/w-0` + `opacity-100/pointer-events-none`) и sidebar content div (`block/hidden`) сега користат helper класи наместо inline ternary, без runtime промени; додаден `src/features/editor/sidebarState.test.js` со **2 тестови** (container open/closed class, content open/closed class); focused tests PASS (**1/1 file, 2/2 tests**), build PASS.
- Progress (2026-05-08 wave 9bt): collab info-badge text derivations се екстрахирани од `src/main.jsx` во нов helper модул `src/features/collab/statusBadges.js` преку `buildCollabStatusBadgeText(...)`, `buildOnlineCollaboratorsBadgeText(...)` и `buildLastEditBadgeText(...)`; navbar badge блокот сега користи helper calls наместо inline string-assembly за transport/session, online participants и last-edit текст, без runtime промени; додаден `src/features/collab/statusBadges.test.js` со **3 тестови** (supported/unsupported status, collaborator names+fallback, last-edit formatter passthrough); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bs): action-button class derivations за navbar controls се екстрахирани од `src/main.jsx` во нов helper модул `src/features/editor/actionButtons.js` преку `getLanguageToggleButtonClass(isActive)`, `getSaveButtonClass(isSaving)` и `getCollabToggleButtonClass(isEnabled)`; `MK/AL` language pills, `save` и `collab` копчињата сега користат helper calls наместо inline ternary class chains, без runtime промени; додаден `src/features/editor/actionButtons.test.js` со **3 тестови** (language/save/collab active-state class derivation); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9br): nav view-tab derivation е екстрахирана од `src/main.jsx` во нов helper модул `src/features/editor/navViewTabs.js` преку `NAV_VIEW_TABS`, `getNavViewLabel(view)` и `getNavViewButtonClass(isActive)`; top nav tabs сега рендерираат преку helper list/label/class наместо inline array + ternary chain, без runtime промени; додаден `src/features/editor/navViewTabs.test.js` со **3 тестови** (tab order, label mapping + fallback, active/inactive class derivation); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bq): advanced settings toggle/layout derivation е екстрахирана од `src/main.jsx` во нов helper модул `src/features/editor/advancedSettings.js` преку `getAdvancedToggleButtonClass(isActive)`, `getNextLayout(layout)` и `getLayoutToggleLabel(layout)`; четирите toggle копчиња (`zipGrade`, `subNumbering`, `layout`, `showScale`) сега користат helper class derivation наместо дуплирани inline ternary класи, а layout toggle switch/label користи helper calls без runtime промени; додаден `src/features/editor/advancedSettings.test.js` со **3 тестови** (active/inactive class derivation, next-layout mapping, layout label mapping); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bp): дуплираната grade-list derivation `[5, 4, 3, 2]` е екстрахирана од `src/main.jsx` во нов helper модул `src/features/editor/gradingScale.js` преку `GRADING_SCALE_GRADES` и `getGradingScaleGradesForDisplay()`; и двете render локации (sidebar quick scale + paper scale panel) сега мапираат преку helper наместо inline literal array, без runtime промени; додаден `src/features/editor/gradingScale.test.js` со **3 тестови** (grade order, getter output, defensive new-array instance); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bo): section-layout grid/span class derivation е екстрахирана од `src/main.jsx` во нов helper модул `src/features/editor/sectionGrid.js` преку `getSectionGridClass(layout)` и `getSectionItemSpanClass(layout, fullWidth)`; section render блокот сега користи helper calls наместо inline ternary за `grid-cols-2/grid-cols-1` и `col-span-2` без runtime промени; додаден `src/features/editor/sectionGrid.test.js` со **3 тестови** (double/single grid classes, fullWidth span rules); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bn): view-branching derivation за test-paper content (`analytics/verify/answerSheet/default`) е екстрахирана од `src/main.jsx` во нов helper модул `src/features/editor/viewMode.js` преку `determinePaperContentView(view)` и `getPaperContentContainerClass(contentView)`; `main.jsx` сега користи memoized `paperContentView` и helper-based class derivation наместо inline conditional chaining, без runtime промени; додаден `src/features/editor/viewMode.test.js` со **3 тестови** (known view mapping, default fallback, answerSheet class toggle); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bm): answer-sheet option/label derivation логиката е екстрахирана од `src/main.jsx` во нов helper модул `src/features/editor/answerSheet.js` преку `shouldRenderAnswerSheetOptions(type)` и `getAnswerSheetOptionLabels(question)`; inline ternary блокот (`true-false`/`options` + alphabetic labels) во answer-sheet render branch е заменет со helper calls без runtime промени; додаден `src/features/editor/answerSheet.test.js` со **3 тестови** (supported types gating, `['Т','Н']` labels, alphabetic option labels); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bl): inline section-layout preparation блокот (`sections/currentSection` builder за header + grouped question секции) е екстрахиран од `src/main.jsx` во нов pure helper модул `src/features/editor/sectionLayout.js` преку `buildQuestionSections(questions, defaultLayout)`; render branch во `main.jsx` сега користи helper call без runtime промени и ја зачувува trailing-empty-section compatibility; додаден `src/features/editor/sectionLayout.test.js` со **3 тестови** (single section baseline, section header split + layout override, trailing empty section case); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bk): повторливата tutorial highlight class-selection логика (`showTutorial && tutorialStep === X ? class : ''`) е екстрахирана од `src/main.jsx` во нов helper модул `src/features/editor/tutorialHighlights.js` преку `getTutorialStepHighlightClass(showTutorial, tutorialStep, targetStep, activeClass)`; заменети се highlight call-sites за `main-nav`, `action-buttons`, `toolbox-sidebar`, `bank-tab`, `advanced-settings`, `test-paper` без runtime промени; додаден `src/features/editor/tutorialHighlights.test.js` со **3 тестови** (match, hidden, mismatch); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bj): toolbox section grouping/dedup логиката (`items` + `finalItems` за `geometry` и `напредни`) е екстрахирана од `src/main.jsx` во нов pure helper модул `src/features/editor/toolboxSections.js` преку `buildToolboxSections(filteredTypes, TOOLBOX_CATEGORY_ORDER)`; `main.jsx` сега рендерира преку `toolboxSections` derivation без runtime промени; додаден `src/features/editor/toolboxSections.test.js` со **3 тестови** (section presence, advanced dedup, geometry inclusion); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bi): `filteredTypes` subject-aware search/sort/category логиката е екстрахирана од `src/main.jsx` во нов pure helper модул `src/features/editor/filterTypes.js` преку `buildFilteredQuestionTypes(...)`; `main.jsx` сега користи thin `useMemo` wrapper со истите inputs (`questionTypes`, `typeSearch`, `subject`, `activeCategory`) без runtime промени; додаден `src/features/editor/filterTypes.test.js` со **3 тестови** (case-insensitive search, STEM relevance priority, category filter со `all` fallback); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bh): `questionTypes` inline metadata+icon wiring е екстрахиран од `src/main.jsx` во нов helper модул `src/features/editor/questionTypeConfig.js` преку `QUESTION_TYPE_DEFINITIONS` и `buildQuestionTypes()`; `main.jsx` сега користи `useMemo(() => buildQuestionTypes(), [])` без runtime промени; додаден `src/features/editor/questionTypeConfig.test.js` со **3 тестови** (stable id order, renderable icon nodes, required metadata shape); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bg): `tutorialSteps` static walkthrough блокот е екстрахиран од `src/main.jsx` во нов helper модул `src/features/editor/tutorialConfig.js` преку `TUTORIAL_STEPS`; `main.jsx` е префрлен на import-based wiring (`const tutorialSteps = TUTORIAL_STEPS`) без runtime промени; додаден `src/features/editor/tutorialConfig.test.js` со **3 тестови** (target order, complete shape, unique IDs); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bf): `categoryIcons` и `categories` inline UI configuration е екстрахирана од `src/main.jsx` во нов helper модул `src/features/editor/categoryConfig.js` преку `buildCategoryIcons()`, `CATEGORY_FILTERS` и `TOOLBOX_CATEGORY_ORDER`; sidebar category-chip render и toolbox category loop сега користат import-based wiring без runtime промени; додаден `src/features/editor/categoryConfig.test.js` со **3 тестови** (filter order, category order, valid icon nodes); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9be): `helpContent` static guidance dictionary е екстрахиран од `src/main.jsx` во нов helper модул `src/features/editor/helpContent.js` (`HELP_CONTENT`) за понатамошно намалување на shell тежината и подобра модуларност без runtime промени; `main.jsx` е префрлен на import-based wiring (`const helpContent = HELP_CONTENT`); додаден `src/features/editor/helpContent.test.js` со **3 тестови** (expected keys, shape completeness, legacy copy check); focused tests PASS (**1/1 file, 3/3 tests**), build PASS.
- Progress (2026-05-08 wave 9bd): full gate е затворен по стабилизација на test setup path во `vite.config.js` (`setupFiles` насочен кон постоечкиот `src/test/setup.js`); full Vitest suite PASS (**35/35** files, **259/259** tests), production build PASS, и chunking состојбата останува стабилна без large-chunk warning (најголем chunk `vendor-pdf-core` ~**1,441.27 kB** под лимит 1600).
- Progress (2026-05-08 wave 9bc): продолжен bundle optimization чекор со on-demand lazy-loading на export actions во `src/main.jsx` — статичките imports за `downloadTestPdf`/`downloadTestDocx` се заменети со dynamic import loaders (`loadPdfExporter`, `loadDocxExporter`) повикани директно од PDF/DOCX toolbar actions, за да не се влече export стекот во initial runtime path; потоа во `vite.config.js` е извршен granular split на export vendor-ите (`vendor-docx`, `vendor-pdf-renderer`, `vendor-pdf-core`) наместо единечен `vendor-export` chunk; резултат: large chunk warning е исчистен целосно, со нови најголеми export chunks `vendor-pdf-core` ~**1,441.27 kB** и `vendor-docx` ~**349.52 kB** (под `chunkSizeWarningLimit` 1600), build PASS.
- Progress (2026-05-08 wave 9bb): иницијален bundle/chunk optimization чекор во `vite.config.js` преку `build.rollupOptions.output.manualChunks(...)` со прецизни vendor групи (`vendor-react`, `vendor-firebase`, `vendor-export`, `vendor-collab`, `vendor-vision`, `vendor-icons`, `vendor-zod`, `vendor-state`, `vendor-utils`) + feature chunks (`feature-take`, `feature-analytics`, `feature-collab`, `feature-export`, `feature-editor`); првиот нацрт со catch-all `vendor-misc` создаде circular warning и беше веднаш коригиран со отстранување на broad fallback; резултат: circular warning е исчистен, entry chunk е намален на ~**92.81 kB** (`index-*.js`), но останува единечен large-chunk warning за `vendor-export` (~**1,809.69 kB**) поради export стекот (`@react-pdf/renderer`/`docx` екосистем); typecheck чист, full suite **71/71** files / **610/610** тестови PASS, build PASS.
- Progress (2026-05-08 wave 9ba retry): во `main.jsx` е исчистен преостанатиот hardcoded grading-scale render list (`[5,4,3,2]`) и е заменет со постоечкиот helper `getGradingGradesForDisplay()` за целосна конзистентност со editor helper слојот; runtime однесувањето е идентично, но view render derivation е целосно helper-driven; focused tests PASS (**3/3 files, 72/72 tests**), typecheck чист, full suite **71/71** files / **610/610** тестови PASS, build PASS.
- Progress (2026-05-08 wave 9az): финален completeness-pass cleanup во `main.jsx` за преостанати UI conditionals — нови helper-и во `src/features/editor/buttonStates.ts` (`getEditorToggleIconName(...)`, `getSidebarToggleIconName(...)`), `src/features/editor/previewPrintUi.ts` (`getSidebarContainerClass(...)`, `getSidebarContentClass(...)`) и `src/features/editor/uiConfig.ts` (`getCategoryFilterButtonClass(...)`); `main.jsx` сега ги користи helper derivations за sidebar visibility/icon, editor-toggle icon, category filter button class и paste-modal close consistency (`closePasteModal`), со без runtime промени; test coverage проширен во `src/features/editor/buttonStates.test.ts` (**41 тестови**), `src/features/editor/previewPrintUi.test.ts` (**15 тестови**) и `src/features/editor/uiConfig.test.ts` (**25 тестови**); focused tests PASS (**3/3 files, 81/81 tests**), typecheck чист, full suite **71/71** files / **610/610** тестови PASS, build PASS.
- Progress (2026-05-08 wave 9ay): дополнително исчистени преостанати UI derivations во `main.jsx` преку проширување на `src/features/editor/questionTypeHelpers.ts` — додадени `findQuestionTypeById(...)`, `shouldShowSubjectIndicator(...)`, `getSubjectIndicatorColorClass(...)`; toolbox subject-indicator dot логиката и floating quick-add type lookup сега користат helper calls наместо inline `includes(...)` и двојни `find(...)`; test coverage проширен во `src/features/editor/questionTypeHelpers.test.ts` на **55 тестови**; focused tests PASS (**3/3 files, 83/83 tests**), typecheck чист, full suite **71/71** files / **602/602** тестови PASS, build PASS.
- Progress (2026-05-08 wave 9ax): преостанатата inline JSON/QTI file-import normalization логика е извадена од `main.jsx` во нов typed helper модул `src/features/editor/fileImports.ts` преку `buildQuestionsFromJsonFileContent(...)` и `buildQuestionsFromQtiXmlContent(...)`; `main.jsx` file-reader handlers се сведени на thin glue (read + helper call + state update) без runtime промени; додаден нов unit пакет `src/features/editor/fileImports.test.ts` со **5 тестови** (JSON defaults/errors и QTI parsing/empty cases); focused tests PASS (**3/3 files, 34/34 tests**), typecheck чист, full suite **71/71** files / **597/597** тестови PASS, build PASS.
- Progress (2026-05-08 wave 9aw): преостанатите inline UI ternary блокови во `main.jsx` се дополнително исчистени преку helper extraction во постоечки editor модули — во `src/features/editor/tutorial.ts` додадени се `getTutorialProgressDotClass(...)`, `isLastTutorialStep(...)`, `getTutorialNextButtonLabel(...)`, `getNextTutorialStep(...)`, а во `src/features/editor/uiConfig.ts` додадени се `getViewTabButtonClass(...)` и `getLanguageButtonClass(...)`; `main.jsx` е префрлен да ги користи овие helpers за tutorial progress/next flow и active класи за view/language pills без runtime промени; test coverage проширен во `src/features/editor/tutorial.test.ts` (**10 тестови**) и `src/features/editor/uiConfig.test.ts` (**23 тестови**); focused tests PASS (**3/3 files, 70/70 tests**), typecheck чист, full suite **70/70** files / **592/592** тестови PASS, build PASS.
- Progress (2026-05-08 wave 9at): editable header input state логиката е до-екстрахирана од `main.jsx` во нов typed helper модул `src/features/editor/headerInputs.ts` преку `buildSchoolHeaderValue(...)`, `parseSchoolHeaderValue(...)`, `applySchoolHeaderInput(...)`, `applySubjectHeaderInput(...)`, `applyGradeHeaderInput(...)`; inline `onChange` трансформациите за school/subject/grade се заменети со helper calls без runtime промени; додаден `src/features/editor/headerInputs.test.ts` со **6 тестови** (build/parse/apply и compatibility случаи); focused tests PASS (**3/3 files, 20/20 tests**), typecheck чист, full suite **70/70** files / **584/584** тестови PASS, build PASS.
- Progress (2026-05-08 wave 9ar): paste/import modal flow е до-екстрахиран од `main.jsx` во нов typed helper модул `src/features/editor/pasteImport.ts` преку `buildQuestionsFromPastedJson(rawJson)` (JSON parse + normalization) и `buildPasteModalClosedState()` (close/reset transition), а `main.jsx` е префрлен на thin handlers `importPastedQuestions()` и `closePasteModal()` без inline parse/map блок; додаден `src/features/editor/pasteImport.test.ts` со **6 тестови** (normalization, defaults, non-multiple behavior, passthrough fields, invalid payload throw); focused tests PASS (**3/3 files, 51/51 tests**), typecheck чист, full suite **69/69** files / **578/578** тестови PASS, build PASS.
- Progress (2026-05-08 wave 9ap-9aq batch): answer-sheet и section-layout preparation логиката е до-екстрахирана од `main.jsx` во нов typed helper модул `src/features/editor/viewContentBuilders.ts` преку `buildAnswerSheetRows(questions)` и `buildQuestionSections(questions, defaultLayout)`; `main.jsx` е префрлен на memoized derivations (`answerSheetRows`, `questionSections`) и ги рендерира без inline grouping/map логика; додаден нов unit пакет `src/features/editor/viewContentBuilders.test.ts` со **8 тестови** (option labels, true/false labels, ordering, section splitting, layout fallback, trailing empty-section compatibility); focused tests PASS (**3/3 files, 44/44 tests**), typecheck чист, full suite **68/68** files / **572/572** тестови PASS, build PASS.
- Progress (2026-05-08 wave 9ak-9ao batch): до-врзани се преостанатите helper wiring точки во `main.jsx` без runtime промени — `determineViewContentType(view)` сега е централизиран derivation за content branching (`analytics/verify/answerSheet/normal`), а advanced-settings toggle buttons за `layout` и `showScale` добија explicit active-state wiring преку `isLayoutButtonActive(...)` и `isShowScaleButtonActive(...)` (`aria-pressed`), додека shuffle и student-line/view logic остануваат helper-driven; focused tests PASS (**4/4 files, 77/77 tests**), typecheck чист, full suite **67/67** files / **564/564** тестови PASS, build PASS.
- Progress (2026-05-08 wave 9aj): editor/preview toggle target-view логиката е до-екстрахирана од `main.jsx` во `src/features/editor/buttonStates.ts` преку нов pure helper `getEditorToggleNextView(view)`; inline ternary во navbar toggle click handler е заменет со helper call без runtime промени; `src/features/editor/buttonStates.test.ts` проширен на **37 тестови** со нов coverage за next-view toggle cases; typecheck чист, focused tests PASS, build PASS, full suite **67/67** files / **564/564** тестови PASS.
- Progress (2026-05-08 security hardening): Firestore rules за `published*` flow се затегнати по review — `publishedTests` сега има owner-bound create/update (`ownerUid == request.auth.uid`), `publishedAttachmentSessions` има immutable proof fields (`payload/signature/signedBy`) и ограничен status transition (`pending -> ready`), а `publishedResumes`/`publishedQuestionAttachments`/`publishedAttempts`/`publishedAuditEvents` добија basic shape checks за create/update за да се намали tampering површина; typecheck чист, build PASS, full suite **67/67** files / **562/562** тестови PASS.
- Progress (2026-05-08 wave 9ai): `ZipGrade` и `Под-нумерирање` advanced-settings button-state class логиката е до-екстрахирана од `main.jsx` во `src/features/editor/advancedSettings.ts` преку `getZipGradeButtonClass(zipGrade)` и `getSubNumberingButtonClass(subNumbering)`; `src/features/editor/advancedSettings.test.ts` проширен со нови тестови (active/inactive + consistency); typecheck чист, focused editor tests PASS, build PASS, full suite **67/67** files / **562/562** тестови PASS.
- Progress (2026-05-08 wave 9ah): editor/preview toggle label logic е до-екстрахирана во `buttonStates` преку i18n-capable `getEditorToggleLabel(view, labels)` (со default fallback за MK labels) и `main.jsx` сега ја користи helper функцијата наместо inline ternary label; `handleStart` е стабилизиран за да нема двојно повикување на `buildSessionStart` со потенцијално stale state; cloud-save success alert flow е порамнет на централизираниот `setTemporaryState` pattern (без inline timeout во `saveCurrentTest` finally); `src/features/editor/buttonStates.test.ts` проширен со custom-label test; full suite **67/67** test files / **556/556** тестови PASS, typecheck чист, build поминува.
- Progress (2026-05-08 wave 9ab): view tabs и language options UI configuration се екстрахирани од `main.jsx` во нов typed helper `src/features/editor/uiConfig.ts` (`VIEW_TABS` const [6 tabs] + `getViewTabsForDisplay()` + `getViewTabLabel(tab)`, `UI_LANGUAGES` const ['mk','sq'] + `getUILanguagesForDisplay()` + `getLanguageLabel(lang)`); view tabs ternary nest се замени со `getViewTabLabel(v)`, а language buttons се рефакторирани во `.map()` loop; додаден `src/features/editor/uiConfig.test.ts` со **19 тестови** (export, order, display, fresh arrays, label mapping); full suite **62/62** test files / **428/428** тестови PASS, typecheck чист, build поминува.
- Progress (2026-05-08 wave 9aa): grading grades display constants се екстрахирани од `main.jsx` во нов typed helper `src/features/editor/gradingGrades.ts` (`GRADING_GRADES` const array [5,4,3,2] + `getGradingGradesForDisplay()` helper); `main.jsx` грид `{[5, 4, 3, 2].map(...)}` е замениво со `{getGradingGradesForDisplay().map(...)}`; додаден `src/features/editor/gradingGrades.test.ts` со **6 тестови** (export, length, order, fresh array); full suite **61/61** test files / **409/409** тестови PASS, typecheck чист, build поминува.
- Progress (2026-05-08 wave 9z): time display formatting се екстрахирани од `main.jsx` во нов typed helper `src/features/editor/timeDisplay.ts` (`buildEstimatedTimeDisplay(minutes)` + `formatTimeDuration(ms)` за future collab use); `main.jsx` сега користи `buildEstimatedTimeDisplay(estimatedTime)` наместо inline `estimatedTime} мін.` literal; додаден `src/features/editor/timeDisplay.test.ts` со **10 тестови** (digit/zero/large values за time, boundary cases за duration parsing); full suite **60/60** test files / **403/403** тестови PASS, typecheck чист, build поминува.
- Progress (2026-05-08 wave 9y): editor notification message constants се екстрахирани од `main.jsx` во нов typed helper `src/features/editor/notificationMessages.ts` (`EDITOR_NOTIFICATIONS` const + `getNotificationMessage()` helper); hard-coded strings (`'Додадено во банката!'`, `'Прашањата се измешани!'`, `'Успешно увезени задачи!'`, `'Вратена е претходната верзија'`, `'Грешка при зачувување.'`) се замени со констант референци; додаден `src/features/editor/notificationMessages.test.ts` со **7 тестови** (constant export, retrieval, all keys); full suite **59/59** test files / **393/393** тестови PASS, typecheck чист, build поминува.
- Progress (2026-05-08 wave 9x): collab status badge text (`RTDB/Local • sessionId`) е екстрахирана од `main.jsx` во нов typed helper `src/features/collab/collabStatus.ts` (`buildCollabStatusText(transport, sessionId, supported)`); `main.jsx` дирекно ги замени inline конде со helper call; додаден `src/features/collab/collabStatus.test.ts` со **6 тестови** (cloud/local transport, supported/unsupported fallback, empty session, unknown transport); full suite **58/58** test files / **386/386** тестови PASS, typecheck чист, build поминува.
- Progress (2026-05-08 wave 9w): toolbox category grouping/dedup логиката е екстрахирана од `main.jsx` во `src/features/editor/toolboxGroups.ts` преку pure helper `buildToolboxGroups()` + `TOOLBOX_CATEGORY_ORDER`; geometry-dup suppression rule за `напредни` е зачувано 1:1, а UI мапира директно преку `toolboxGroups`; додаден `src/features/editor/toolboxGroups.test.ts` со **6 тестови** (order, empty categories, geometry inclusion, dedup for `напредни`, non-geometry behavior); full suite **57/57** test files / **380/380** тестови PASS, typecheck чист, build поминува.
- Progress (2026-05-08 wave 9v): collab presence display derivations се екстрахирани од `main.jsx` во `src/features/collab/presenceView.ts` преку pure helpers `deriveActiveCollaborators`, `buildOnlineCollaboratorsText`, `buildLastRemoteEditText`; `main.jsx` повеќе нема inline string-assembling за Online/Last edit badges, туку користи memoized helper calls; додаден `src/features/collab/presenceView.test.ts` со **8 тестови** (active/recent pruning, zero+named fallback text, last-edit formatting/fallback); full suite **56/56** test files / **374/374** тестови PASS, typecheck чист, build поминува.
- Progress (2026-05-08 wave 9u): `questionTypes` JSX icon mapping е екстрахиран од `main.jsx` во `src/features/editor/questionTypeIcons.tsx` преку helper `buildQuestionTypes()` (metadata од `QUESTION_TYPE_DATA` + React icon nodes); `main.jsx` сега користи `useMemo(() => buildQuestionTypes(), [])` без inline `iconMap`; нов `src/features/editor/questionTypeIcons.test.tsx` со **4 тестови** (length parity, metadata passthrough, valid React elements, order stability); full suite **55/55** test files / **366/366** тестови PASS, typecheck чист, build поминува.
- Progress (2026-05-08 wave 9t): JSX category-icon derivation е извадена од `main.jsx` во `src/features/editor/categoryIcons.tsx` преку helper `buildCategoryIcons()` кој ги мапира `CATEGORY_ICON_DATA` и `lucide-react` икони; `main.jsx` сега има само `useMemo(() => buildCategoryIcons(), [])`; нов `src/features/editor/categoryIcons.test.tsx` со **3 тестови** (key parity, valid React elements, className passthrough); full suite **54/54** test files / **362/362** тестови PASS, typecheck чист, build поминува.
- Progress (2026-05-08 wave 9s): `filteredTypes` useMemo логиката (search + subject-aware relevance sort + category filter) е екстрахирана од `main.jsx` во typed pure helper `src/features/editor/filterTypes.ts` (`filterQuestionTypes<T>()`); `main.jsx` сега има thin useMemo wrapper без inline алгоритам; нов `src/features/editor/filterTypes.test.ts` со **7 тестови** (immutability, search, STEM/lang/geo relevance, priority fallback, category behavior); full suite **53/53** test files / **359/359** тестови PASS, typecheck чист, build поминува.
- Progress (2026-05-08 wave 9r): `categoryIcons` и `categories` inline objects се екстрахирани во `src/features/editor/categoryData.ts` — `CATEGORIES` (5 filter tabs) и `CATEGORY_ICON_DATA` (6 entries со `iconKey`+`className`) со typed interfaces; `main.jsx` ги деривира со прост `Object.fromEntries` + `catIconMap` (само JSX); нов `src/features/editor/categoryData.test.ts` со **7 тестови**; full suite **52/52** test files / **352/352** тестови PASS, typecheck чист, build поминува.

## 🌍 МакедоТест → Глобална платформа: Акционен План

> Визија: Светски водечка платформа за **формативно и сумативно** оценување, со балансиран фокус врз **дигитално решавање** и **печатени тестови**, со предност во STEM-рендерирање и локализација.

---

## 📊 Status Quo (полазна точка)

**Силни страни:** 16 типови задачи, KaTeX/STEM, А4 print + ZipGrade, Firebase auth/Firestore, AI JSON import, QTI извоз, mk/sq.

**Резидуален технички долг (реален):**

- TypeScript миграцијата е уште во тек (нови critical модули треба да почнат во `.ts/.tsx`).
- `src/main.jsx` уште има преостаната декомпозиција (`features/editor`, `features/preview`, `features/print`, `features/bank`).
- Production rollout hardening за secure-evidence flow бара operational runbook и smoke gates по deploy.
- Chunk size е над target (`vite build` warning), потребен контролираn code-splitting план.

---

## 🚦 Roadmap по фази

### Фаза 1 — Foundation Hardening (1–2 месеци)

Цел: одржливо и безбедно за тим од 3+ инженери.

- [x] **1.1** Tooling: ESLint + Prettier + EditorConfig + lint-staged + Husky (инсталирани)
- [x] **1.2** Vitest 2.x + React Testing Library + jest-dom (Node 20/22/24 compat)
- [ ] **1.3** TypeScript migration (постепено, нов код во `.tsx`)
  - Progress (2026-05-07): `src/features/take/proofClient.ts` и `src/features/take/attachments.ts` мигрирани во TypeScript како прв critical secure-evidence slice.
  - Progress (2026-05-07): `src/features/take/actorContext.ts` мигриран; воведен `npm run typecheck` gate (`tsc --noEmit`) и иницијално поминува.
  - Progress (2026-05-07): `src/features/take/publish.ts` мигриран (publish/resume/attachment/attempt/audit helpers + subscriptions), со зелени таргетирани тестови и typecheck.
  - Progress (2026-05-07): `src/features/take/route.ts` мигриран (take/upload route parsing + URL builders), со зелени route tests и typecheck.
  - Progress (2026-05-07): `src/features/take/TakeRoute.tsx` мигриран со типизирана state машина + нов `TakeRoute` unit test coverage; typecheck/build остануваат зелени.
  - Progress (2026-05-07): secure-evidence helper extraction од `StudentTake.jsx` продолжен во `src/features/take/secureEvidence.ts` и `src/features/take/attachmentWorkflow.ts` (proof/gate + session/record payload builders), со нови unit тестови и без runtime регресии.
  - Progress (2026-05-07): submission/audit orchestration дополнително декомпониран во `src/features/take/auditWorkflow.ts` (attempt/gate/upload audit event builders + attachment merge helper), интегриран во `StudentTake.jsx` со зелени таргетирани тестови, typecheck и build.
  - Progress (2026-05-07): `StudentTake.jsx` → `StudentTake.tsx` — целосна TSX миграција со explicit prop types (`StudentTakeProps`, `Question`, `TestModel`, `UploadGate`, `SubmissionProof`, `AttachmentEntry`, `GradeResult`, `ManualInputProps`, `QuestionViewProps`, …); `RenderContent.d.ts` додаден за типски декларации; сите 275 тестови зелени, typecheck чист, build поминува.
  - Progress (2026-05-07): `draftStorage.js`, `useLockdown.js`, `AttachmentUploadRoute.jsx`, `TeacherVerifyPanel.jsx` → целосно мигрирани во `.ts`/`.tsx` со explicit prop types и извезени type декларации (`LockEventKind`, `LockEvent`, `UseLockdownOptions`, `UseLockdownReturn`, `AttachmentUploadRouteProps`, `VerifyCardProps`, `AttemptRecord`, `AttachmentRecord`, `AuditRecord`); целиот `src/features/take/` slice е 100% TypeScript; 275 тестови зелени, typecheck чист, build поминува.
  - Progress (2026-05-07): `src/features/export/pdfRenderer.jsx` и `src/features/export/downloadTestPdf.jsx` мигрирани во `pdfRenderer.tsx` и `downloadTestPdf.ts` со explicit типови (`TestQuestion`, `TestPdfDocumentProps`, `DownloadTestPdfInput`), import-ите префрлени на extensionless TS resolution; сите 39 test files / 275 tests зелени, typecheck чист, build поминува.
  - Progress (2026-05-07): Batch B+C стабилизација (secure-evidence backend + gate automation) валидирана end-to-end: `functions` тестови зелени (вкл. `uploadHandler`), `gate:secure-evidence:local` PASS, `typecheck` чист; gate runner затегнат за clean execution без Node `DEP0190` warning.
  - Progress (2026-05-07): Deployed secure-evidence smoke PASS на production API (`https://api-z6aqe5ejxq-ew.a.run.app/api`) + full `gate:secure-evidence` PASS со canonical release evidence: `artifacts/releases/secure-evidence-gate-2026-05-07T21-34-44-808Z.md`.
  - Progress (2026-05-07): `src/features/analytics/TeacherAnalyticsPanel.jsx` → `TeacherAnalyticsPanel.tsx` мигриран со explicit props (`TeacherAnalyticsPanelProps`) и strict-safe helper typing; legacy JSX отстранет; analytics тест пакет (6/6) + typecheck + build остануваат зелени.
  - Progress (2026-05-07): `src/features/analytics/demoAttempts.js` → `demoAttempts.ts` мигриран со explicit analytics types (`AnalyticsQuestion`, `DemoAttemptItem`, `DemoAttempt`) и нов focused unit test coverage; legacy JS отстранет; analytics regression пакет (7/7) + typecheck + build остануваат зелени.
  - Progress (2026-05-07): `bloom.js`, `cohortAnalysis.js`, `ragQuality.js` → `bloom.ts`, `cohortAnalysis.ts`, `ragQuality.ts` мигрирани со literal const unions (`BloomLevel`, `RagFeedback`) и explicit return types (`BloomCoverage`, `CohortAnalysisResult`, `SemesterDelta`, `RagQualityRecord`, `RagQualitySummary`); legacy JS отстранети; `TeacherAnalyticsPanel.tsx` патчиран за narrowed BloomLevel indexing; analytics пакет (7/7 / 28 тестови) + typecheck + build остануваат зелени.
  - Progress (2026-05-08): `itemAnalysis.js`, `spreadsheetExport.js`, `pdfReports.js` → `itemAnalysis.ts`, `spreadsheetExport.ts`, `pdfReports.ts` мигрирани со explicit типови (`AttemptItem`, `Attempt`, `ItemStat`, `CohortSummary`, `AttemptAnalysisResult`, `DifficultyLabel`, `DiscriminationLabel`, `GradeResult`, `SpreadsheetHeader`, `SpreadsheetRow`, `SpreadsheetArtifacts`, `PdfReportInput`); legacy JS отстранети; целиот `src/features/analytics/` slice е 100% TypeScript; **40/40** test files / **277/277** тестови зелени, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 2): `grade.js` → `grade.ts` (`GradeResult`, `GradeItem`, `TestGradeResult`, `GradingScaleTier`, `GradableQuestion`); `editorHistory.js` → `editorHistory.ts` (`EditorSnapshot`, `EditorSnapshotInput`); `embed.js` → `embed.ts` (`DiagramEmbedType`); `json.js` → `json.ts`; `vision.js` → `vision.ts`; `docxExport.js` → `docxExport.ts` (`TestDocxData`, `QuestionLike`); `domain/index.js`, `domain/responsePolicy.js` → `.ts` (`ResponseConfig`); `i18n/index.js`, `i18n/translations.js` → `.ts` (`SupportedLanguage`, `TranslationDict`); `hooks/useAuth.js` → `useAuth.ts` (`AuthState`, `AuthStatus`); тест imports без `.js` наставки поправени; **40/40** test files / **277/277** тестови зелени, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 3): целиот `src/features/collab/` slice мигриран во TypeScript: `yjsCodec.js` → `yjsCodec.ts` (`EditorState`), `session.js` → `session.ts` (`EditorSyncPayload`, `PresencePayload`), `rtdbTransport.js` → `rtdbTransport.ts` (`DbApi`, `RtdbCollabOptions`), `presencePolicy.js` → `presencePolicy.ts`, `conflictHints.js` → `conflictHints.ts`; legacy JS отстранети; collab таргет тестови **24/24** PASS, full suite **40/40** test files / **277/277** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 4): целиот `src/features/rag/` core slice мигриран во TypeScript: `chunking.js` → `chunking.ts` (`TextChunk`, `ChunkOptions`), `retrieval.js` → `retrieval.ts` (`EmbeddingCandidate`), `suggestions.js` → `suggestions.ts` (`RagMatch`), `ragClient.js` → `ragClient.ts` (`IndexRagInput`, `QueryRagInput`); legacy JS отстранети; RAG таргет тестови **10/10** PASS, full suite **40/40** test files / **277/277** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 5): целиот `src/features/curriculum/` slice мигриран во TypeScript: `snapshotSchema.js` → `snapshotSchema.ts` (`CurriculumSnapshot`, `TrackSnapshot`, `Grade`, `Topic`, `Concept`), `importAdapter.js` → `importAdapter.ts` (`AdaptedCurriculum`, `GradeEntry`, `ConceptEntry`), `ragDocuments.js` → `ragDocuments.ts` (`RagDocument`), `validateSnapshot.js` → `validateSnapshot.ts` (`CurriculumValidationResult`, `CurriculumValidationReport`), `reportArtifact.js` → `reportArtifact.ts` (`CurriculumReportArtifact`, `TrackCoverageEntry`); curriculum test imports префрлени на extensionless resolution; legacy JS отстранети; curriculum таргет тестови **19/19** PASS, full suite **40/40** test files / **277/277** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 6): core domain+infra slice мигриран во TypeScript: `domain/schema.js` → `domain/schema.ts` (`Question`, `QuestionList`, `TestModel`), `lib/firebase.js` → `lib/firebase.ts` (typed `FirebaseOptions` config + централизирани `auth/db/rtdb/storage` exports), `store/testStore.js` → `store/testStore.ts` (`TestStoreState`, `StoreTest`); тест imports префрлени на extensionless resolution (`schema.test.js`, `testStore.test.js`); legacy JS отстранети; таргет тестови **20/20** PASS, full suite **40/40** test files / **277/277** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 7): UI components slice мигриран во TSX: `components/RenderContent.jsx` → `RenderContent.tsx`, `components/LandingPage.jsx` → `LandingPage.tsx`, `components/Question.jsx` → `Question.tsx` (strict-safe explicit props/callback typing и зачувано runtime однесување), `Question.test.jsx` import префрлен на extensionless path; legacy `components/*.jsx` и `RenderContent.d.ts` отстранети; component таргет тестови **4/4** PASS, full suite **40/40** test files / **277/277** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 8): `src/components/Question.tsx` дополнително strict-hardening без runtime промени: заменети преостанати `any` со структурни типови (`QuestionModel`, `TestInfoModel`, `MatchItem`, `StatementItem`, `Marker`, `TableData`, `RagMatch`, `HelpEntry`) и стабилизирани optional-array update патеки (`options/items/parts/matches`) со safe fallback spread patterns; резултат: `Question.tsx` без `any`, typecheck чист, component таргет тестови **4/4** PASS, full suite **40/40** test files / **277/277** тестови PASS, build поминува.
  - Progress (2026-05-08 wave 9): test bootstrap мигриран во TypeScript: `src/test/setup.js` → `src/test/setup.ts` (додаден global declaration за `IS_REACT_ACT_ENVIRONMENT` + explicit typing за console guard helpers), `vite.config.js` `setupFiles` префрлен на `./src/test/setup.ts`; legacy `setup.js` отстранет; typecheck чист, component таргет тестови **4/4** PASS, build поминува.
  - Progress (2026-05-08 wave 9b): преостанатите `src/**/*.test.js` unit тестови мигрирани во `.test.ts` (33 фајлови). За да остане strict gate зелен по bulk rename, на 17 комплексни тестови е додаден привремен `// @ts-nocheck` marker (планиран follow-up hardening итеративно по slice); full suite **40/40** test files / **277/277** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9c): старт на phased extraction од `main.jsx` без runtime промени: издвоени editor метрики во нов typed модул `src/features/editor/metrics.ts` (`computeTotalPoints`, `estimateQuestionMinutes`, `computeEstimatedMinutes`) и `main.jsx` префрлен да ги користи; full suite **40/40** test files / **277/277** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9d): продолжен phased extraction од `main.jsx` со static/default блокови во typed editor модул `src/features/editor/defaults.ts` (`HELP_CONTENT`, `createInitialTestInfo`, `createInitialQuestions`); `main.jsx` е исчистен од голем inline literal payload без runtime промени; full suite **40/40** test files / **277/277** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9e): duplicate-detection extraction од `main.jsx` во typed editor helper `src/features/editor/quality.ts` (`normalizeQuestionText`, `findDuplicateQuestionTexts`) + нов unit coverage `src/features/editor/quality.test.ts`; runtime однесувањето останува исто; full suite **41/41** test files / **280/280** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9f): grading-scale extraction од `main.jsx` во `src/features/editor/metrics.ts` (`computeGradingScale`) и додаден editor metrics unit пакет `src/features/editor/metrics.test.ts`; `main.jsx` дополнително поедноставен без runtime промени; full suite **42/42** test files / **284/284** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9g): question-metadata mutation helpers extraction од `main.jsx` во typed editor helper `src/features/editor/questionMeta.ts` (`applyQuestionBloom`, `applyQuestionRagFeedback`) + нов unit coverage `src/features/editor/questionMeta.test.ts` (6 тестови); `main.jsx` handlers дополнително поедноставени без runtime промени; full suite **43/43** test files / **290/290** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9h): keyboard event + list-operation helpers extraction од `main.jsx` во typed модули `src/lib/keyboard.ts` (`triggerOnEnterOrSpace`) и `src/features/editor/listOps.ts` (`reorderQuestions`, `shuffleQuestionOptions`) + нов unit coverage `src/lib/keyboard.test.ts` (3) и `src/features/editor/listOps.test.ts` (9); full suite **45/45** test files / **302/302** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9i): question-creation factory extraction од `main.jsx` во typed editor модул `src/features/editor/questionFactory.ts` (`createQuestion`) + нов unit coverage `src/features/editor/questionFactory.test.ts` (12 тестови, покрива сите 16 типови); `addQuestion` во `main.jsx` сведен на еден ред; full suite **46/46** test files / **314/314** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9j): list-level shuffle extraction од `main.jsx` во постоечкиот typed helper `src/features/editor/listOps.ts` (`shuffleQuestions`), плус отстранет е веќе непотребниот import на `createDefaultResponseConfig` од `main.jsx`; `randomizeQuestions` сега делегира на pure helper, а unit coverage во `src/features/editor/listOps.test.ts` е проширен на 12 тестови; full suite **46/46** test files / **317/317** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9k): undo stack pop extraction од `main.jsx` во typed history модул `src/features/history/editorHistory.ts` (`popUndoSnapshot`), со проширен unit coverage во `src/features/history/editorHistory.test.ts` на 6 тестови; `handleUndo` во `main.jsx` сега користи централизирана history трансформација без runtime промени; full suite **46/46** test files / **319/319** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9l): start-session transition extraction од `main.jsx` во typed editor helper `src/features/editor/sessionStart.ts` (`buildSessionStart`), со нов unit coverage `src/features/editor/sessionStart.test.ts` (4 тестови); `handleStart` е сведен на thin setter glue без runtime промени; full suite **47/47** test files / **323/323** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9m): повторувачкиот duplicate-alert timeout pattern е извлечен од `main.jsx` во typed util `src/lib/temporaryState.ts` (`setTemporaryState`) со нов unit coverage `src/lib/temporaryState.test.ts` (3 тестови); повеќе идентични `setDuplicateAlert` + `setTimeout` call-sites сега делегираат на централизираниот helper без runtime промени; full suite **48/48** test files / **326/326** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9n): `conflictHint` timeout cleanup во `main.jsx` е префрлен на постоечкиот util `src/lib/temporaryState.ts`, со проширен unit coverage во `src/lib/temporaryState.test.ts` на 4 тестови за string-reset случаи; ова дополнително ја унифицира transient UI-state шемата без runtime промени; full suite **48/48** test files / **327/327** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9o): `saveCurrentTest` notification логиката е екстрахирана во `src/features/editor/cloudSave.ts` — два pure typed helpers `buildSavePayload(testInfo, questions, now?)` и `buildSaveMessage(wasExisting: boolean)` заменуваат inline object literal и inline conditional string во `main.jsx`; нов `src/features/editor/cloudSave.test.ts` со **6 тестови**; full suite **49/49** test files / **333/333** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9p): статичната data низа `tutorialSteps` (6 чекори) е екстрахирана од `main.jsx` во `src/features/editor/tutorial.ts` — typed `TutorialStep` interface + `TUTORIAL_STEPS` константа; `main.jsx` ја референцира директно без никакви промени во runtime однесувањето; нов `src/features/editor/tutorial.test.ts` со **6 тестови** (структура, unique targetIds, boundary items); full suite **50/50** test files / **339/339** тестови PASS, typecheck чист, build поминува.
  - Progress (2026-05-08 wave 9q): `questionTypes` useMemo (~80 линии JSX+data) е декомпозиран — статичниот дел (id, label, iconKey, cat, subjects, priority) за сите 16 типови е екстрахиран во `src/features/editor/questionTypeData.ts` со typed `QuestionTypeEntry` interface и `QuestionTypeIconKey` union; `useMemo` во `main.jsx` е заменет со прост `.map()` преку `QUESTION_TYPE_DATA` со inline `iconMap` (само JSX остана во `main.jsx`); нов `src/features/editor/questionTypeData.test.ts` со **6 тестови**; full suite **51/51** test files / **345/345** тестови PASS, typecheck чист, build поминува.
- [x] **1.4** Tailwind локално (PostCSS + autoprefixer + design tokens во `tailwind.config.js`)
- [x] **1.5** `.env.example` + `src/lib/firebase.js` (централизирана, со dev warning за missing keys)
- [x] **1.6** Refactor `main.jsx` → modular: `i18n/`, `lib/firebase`, `domain/schema`, `store/testStore`, `hooks/useAuth`, `features/grading`, `features/import-export`, `features/take` ✓ (преостана: `features/editor`, `features/preview`, `features/print`, `features/bank`)
- [x] **1.7** Domain schema со Zod за сите 16 типови (`src/domain/schema.js`, 9 тестови)
- [x] **1.8** CI: GitHub Actions (`.github/workflows/ci.yml`) — lint + format + test + coverage + build на Node 20/22; PR template
- [x] **1.9** Worktree cleanup discipline: формализиран batch-based редослед за паралелни промени (TS migration / secure-evidence / ops scripts / docs / artifacts) преку ADR-0009 за да нема изгубени или мешани итерации

### Фаза 2 — Student Mode & Auto-Grading (2–3 месеци) ⭐

Најголем impact vs. Quizizz/Kahoot/Google Forms.

- [x] **2.1** Public test links (`/t/ABC123` или `?code=`): `features/take/route.js`, `publish.js` (Firestore /artifacts/{APP_ID}/publishedTests/{code}), `TakeRoute.jsx` mounted во `main.jsx`
- [x] **2.2 (MVP)** Live taking UI: localStorage auto-save, progress bar, a11y semantika (radiogroup/fieldset)
- [x] **2.3** Auto-grading engine со партијален кредит за 8 типа (`src/features/grading/grade.js`, 31 тест)
- [x] **2.4** Manual rubric placeholder (essay/short-answer/fill-blanks/list/diagram/table → `requiresManual: true`)
- [x] **2.5** Lockdown mode (lite): `useLockdown` hook (fullscreen, blur/visibility, copy/paste/cut/contextmenu блокирани и логирани) интегриран во `StudentTake` преку `lockdown` prop (7 тестови)
- [x] **2.6** PWA + IndexedDB offline mode
- [x] **2.7** Resume token (продолжи на друг уред): URL `?r=<token>` + Firestore `publishedResumes` save/load + `StudentTake` token UX
- [x] **2.8** RAG foundation (Google Embeddings 2 via backend proxy): chunking, indexing API, retrieval API, namespace isolation, feature flag rollout
- [x] **2.9** Secure submission chain (Macedonian Dugga-grade): server-signed QR proof + attachment integrity + teacher verify endpoint/panel + server-issued upload slots + immutable audit timeline + realtime teacher monitoring + Firestore/Storage rules enforcement
- [x] **2.10** No-Tech-Debt Production Gate (secure-evidence track)
  - [x] Deploy runbook: secrets (`SUBMISSION_SIGNING_KEY`, `UPLOAD_SLOT_SIGNING_KEY`), rules deploy, rollback path
  - [x] Post-deploy smoke checks: proof sign/verify, slot issue/finalize, blocked upload without verification, teacher live panel (`npm run smoke:secure-evidence`)
  - [x] Ops SLO baseline: API latency/error budget + alert thresholds for submission/upload endpoints
  - [x] Incident playbook: evidence mismatch, expired slots spike, invalid-signature bursts
  - [x] Release evidence (2026-05-07): `artifacts/releases/secure-evidence-gate-2026-05-07T17-58-44-365Z.md`
  - [x] Latest gate evidence refresh (2026-05-07): `artifacts/releases/secure-evidence-gate-2026-05-07T21-34-44-808Z.md`

### Фаза 3 — Teacher Analytics (1–2 месеци)

Валута на B2B продажба кон училишта.

- [x] **3.1** `features/analytics/itemAnalysis.js` + `features/analytics/TeacherAnalyticsPanel.jsx` — pure psychometrics (p-value, point-biserial, cohort stats, deciles) + teacher dashboard UI интеграција во главен app view.
- [x] **3.2** Bloom класификација (рачно/AI-assisted heuristic) → когнитивни извештаи (coverage + manual override + auto-suggest)
- [x] **3.3** Cohort analysis: одделение во семестар (grade x semester breakdown + trend deltas)
- [x] **3.4** PDF извештаи за родители + училишна администрација (print-ready parent/admin templates + export actions)
- [x] **3.5** Excel/Google Sheets извоз за е-дневници (CSV bundle + Google Sheets clipboard TSV)
- [x] **3.6** RAG quality dashboard: retrieval latency, citation coverage, hallucination sampling, teacher feedback loop (interactive feedback controls)

### Фаза 4 — Authoring 2.0 (паралелно со 2/3)

- [x] **4.1** PDF извоз преку `@react-pdf/renderer` (пиксел-перфект, без browser print)
- [x] **4.2** DOCX извоз преку `docx`
- [x] **4.3** Vision Import (GPT-4o + Tesseract fallback): хартиен тест → JSON
- [x] **4.4** GeoGebra/Desmos embed за интерактивни графички задачи
- [x] **4.5** Question version history (Firestore subcollections); тест-ниво Undo
- [x] **4.6** Collaborative editing (Yjs + Firestore RTDB) — modular CRDT codec + local broadcast sync + RTDB transport adapter + presence metadata + conflict UX indicators + stale presence pruning/activity windows
- [ ] **4.7** AI Co-author (предлага дистрактори, тежина, време)

### Фаза 5 — Content Marketplace (3+ месеци)

- [ ] **5.1** Public library со рејтинг + тагови (предмет/одделение/куркулум)
- [ ] **5.2** Curriculum mapping за МК/АЛ/SR/HR/BG/EU стандарди
- [ ] **5.3** Forking & remix
- [ ] **5.4** Revenue share за топ-наставници

### Фаза 6 — Internationalization & Compliance

- [ ] **6.1** i18next (извади ги стринговите од `translations`)
- [ ] **6.2** Јазици: SR, HR, BG, TR, EN
- [ ] **6.3** GDPR: DPA, право на бришење, EU Firestore region
- [ ] **6.4** WCAG 2.1 AA audit
- [ ] **6.5** SOC 2 Type 1 (self-attestation)
- [ ] **6.6** OneRoster + LTI 1.3 (Canvas, Moodle, Google Classroom)

### Фаза 7 — Differentiation

- [ ] **7.1** STEM-first позиционирање (KaTeX advantage)
- [ ] **7.2** Print-first DNA (70% училишта печатат)
- [ ] **7.3** AI-генериран варијантен тест (анти-препишување)
- [ ] **7.4** Adaptive testing (IRT) — премиум
- [ ] **7.5** Plagiarism / similarity check

---

## 💼 Бизнис-модел

| Слој | Опис | Цена |
| --- | --- | --- |
| Free | До 3 тестови, водомарка | €0 |
| Teacher Pro | Неограничено, analytics, AI import | €5–8/месец |
| School | SSO, admin dashboard, LTI | €2/ученик/година |
| District / Ministry | On-prem, custom curriculum | Enterprise |

**Go-to-market:** МК → АЛ → Балкан → CEE → EU → Global STEM niche.

---

## 🎯 Прв sprint (овој месец) — без компромиси

1. **Tooling setup** (ESLint, Prettier, EditorConfig, Husky, .env.example)
2. **Test infrastructure** (Vitest + RTL)
3. **Modularization round 1:** извади `i18n`, `firebase`, `domain/schema` од `main.jsx`
4. **CI** (GitHub Actions: lint + test + build на secret-аware preview)
5. **Student Mode MVP** прототип (read-only такинг + auto-grade за multiple/true-false/checklist)
6. **RAG pilot** (1 предмет, 1 одделение, 3 наставници): Google Embeddings 2 + retrieval eval + guardrails

---

**Одговорни:** Игор Богданоски (продукт/архитектура) + Zencoder AI (имплементација).
**Журнал на прогрес:** секоја фаза заклучува со tag во git и кратка ретроспектива во овој фајл.

---

## 🔐 Risk Register (активен)

- **R1: Misconfigured secrets/rules во production**
  - Mitigation: `2.10` deploy runbook + mandatory smoke gate
- **R2: Companion upload злоупотреба без верификација**
  - Mitigation: upload lock додека QR proof не е `VALID` + backend slot/finalize checks
- **R3: Forensic gap при инцидент**
  - Mitigation: immutable `publishedAuditEvents` + device/session fingerprints + teacher verify timeline
- **R4: Runtime regressions по hardening**
  - Mitigation: full FE suite + full Functions suite + production build како release gate
